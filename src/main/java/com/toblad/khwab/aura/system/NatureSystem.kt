package com.toblad.khwab.aura.system

import com.toblad.khwab.aura.engine.FrameClock
import com.toblad.khwab.aura.world.AuraWorld

/**
 * Updates natural elements such as trees,
 * leaves, flowers and wildlife.
 */
class NatureSystem : AuraSystem {

    override fun update(
        world: AuraWorld,
        clock: FrameClock
    ) {
    }
}
