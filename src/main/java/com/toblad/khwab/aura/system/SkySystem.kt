package com.toblad.khwab.aura.system

import com.toblad.khwab.aura.engine.FrameClock
import com.toblad.khwab.aura.world.AuraWorld

/**
 * Updates celestial objects such as the sun,
 * moon and stars.
 */
class SkySystem : AuraSystem {

    override fun update(
        world: AuraWorld,
        clock: FrameClock
    ) {
    }
}
