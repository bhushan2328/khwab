# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8

# AGP 9.x uses built-in Kotlin; this flag allows KSP to register its generated
# source sets without a configuration error during the transition period.
android.disallowKotlinSourceSets=false
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# When enabled, the Configuration Cache allows Gradle to skip the configuration
# phase entirely if nothing that affects the build configuration (such as build scripts)
# has changed. Additionally, Gradle applies performance optimizations to task execution.
# Configuration cache is disabled: Unity's unityLibrary/build.gradle accesses
# Task.project at execution time (buildIl2Cpp task), which is incompatible with
# Gradle's configuration cache. This can be re-enabled once Unity updates their template.
org.gradle.configuration-cache=false
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8

# Unity library properties (required by khwab-aura-unity/export/unityLibrary/build.gradle)
unityStreamingAssets=.json
unity.buildToolsVersion=36.0.0
unity.androidSdkPath=C:/Program Files/Unity/Hub/Editor/6000.5.7f1/Editor/Data/PlaybackEngines/AndroidPlayer/SDK
unity.androidNdkPath=C:/Program Files/Unity/Hub/Editor/6000.5.7f1/Editor/Data/PlaybackEngines/AndroidPlayer/NDK
unity.androidNdkVersion=27.2.12479018
unity.installInBuildFolder=false
unity.debugSymbolLevel=none
